<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/multichoice"
    tools:context="com.example.wangsang.toeicproject.MainActivity"
    android:weightSum="1">

    <Button
        android:id="@+id/btnReading"
        android:background="@drawable/btn_reading"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height"
        android:layout_marginTop="250dp"
        android:layout_marginBottom="@dimen/gap" />

    <Button
        android:id="@+id/btnListening"
        android:layout_below="@+id/btnReading"
        android:background="@drawable/btn_listening"
        android:layout_alignLeft="@+id/btnReading"
        android:layout_alignStart="@+id/btnReading"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height"
        android:layout_marginBottom="@dimen/gap" />

    <Button
        android:id="@+id/btnVolcabulary"
        android:background="@drawable/btn_vocabulary"
        android:layout_below="@+id/btnListening"
        android:layout_alignLeft="@+id/btnReading"
        android:layout_alignStart="@+id/btnReading"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height"
        android:layout_marginBottom="@dimen/gap" />

    <Button
        android:id="@+id/btnGrammar"
        android:background="@drawable/btn_grammar"
        android:layout_below="@+id/btnVolcabulary"
        android:layout_alignLeft="@+id/btnReading"
        android:layout_alignStart="@+id/btnReading"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height" />

    <TextView
        android:id="@+id/txvToeic"
        android:text="@string/app_name"
        android:textSize="50sp"
        android:textColor="@color/Blueq"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
